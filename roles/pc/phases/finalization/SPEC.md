# PC Finalization Phase

Entry requires one same-arm SAC bundle with exactly seven ordered unique slots. Validate procedural completion and calibration provenance. Preserve typed failures. Produce exactly one benchmark-only `accept` or `reject` for each calibrated row, with reason, evidence, meta-review and SAC references, unresolved dissent, and immutable hash. Publish all seven per-slot artifacts before the arm bundle. Spotlight fields and outcomes are forbidden.
