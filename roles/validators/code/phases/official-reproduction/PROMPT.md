Freeze the official artifact, apply `EXECUTION_POLICY.md`, and execute the cheapest meaningful static command plan through an approved backend. Prefer local Docker; use one bounded VESSL batch job only for a recorded GPU escalation reason and only when its image and inputs are ready. Within the nine-minute executor budget, prioritize static verification, cached-environment import/build checks, one minimal execution probe, and at most one small claim spot-check. Record provenance, license, backend, image, command, hardware, datasets, logs, hashes, result, cost, cleanup, and limitations. Do not retry, run repository code on the host, perform full training, or claim an unavailable central result was reproduced.
