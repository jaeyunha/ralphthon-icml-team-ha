Use only the manifest-declared paper, supplement, algorithm/equation, and environment evidence. Apply the remaining executor budget from `EXECUTION_POLICY.md`; do not reset the nine-minute clock. Independently implement only a minimal claim-critical component that can be completed and self-tested within the remaining time. Otherwise preserve the strongest achieved status and record `budget_exhausted` with the unimplemented scope as a limitation. Do not inspect any official or third-party implementation. Freeze any completed tree before requesting comparison access.
