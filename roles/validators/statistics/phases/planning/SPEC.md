# Planning Phase

Select anchored empirical claim targets and enumerate required checks: runs, seeds, uncertainty, inference, leakage, split integrity, baseline parity, metrics, claim breadth, and robustness axes. Produce a phase-local audit plan; do not issue findings yet.
