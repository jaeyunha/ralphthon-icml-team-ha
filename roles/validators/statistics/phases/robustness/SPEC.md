# Robustness Phase

Record which axes were actually tested among seeds, splits, hyperparameters, datasets, backbones, compute, scale, corruption, imbalance, shift, ablations, baseline choices, and hardware. Compare claim breadth with evidence breadth.
