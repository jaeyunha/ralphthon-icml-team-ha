# Symbolic Validation Phase Prompt

Choose the exact tool matching the current claim. Preserve the original and normalized expressions, declared symbols/domains, tool version, and raw result. For SMT, solve the negated implication and retain a model when satisfiable. For shape and equation-to-code checks, state the interface assumptions. A nonzero symbolic difference is evidence, not permission to overstate scope.
