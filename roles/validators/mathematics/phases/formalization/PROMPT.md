# Formalization Phase Prompt

Formalize only the current anchored theorem. Before compilation, write explicit paper semantics, formal semantics, and alignment evidence. Run the proof in the pinned network-disabled Lean container. Preserve source and compiler evidence. If an easier or different theorem compiles, report statement mismatch; do not call the paper theorem verified.
