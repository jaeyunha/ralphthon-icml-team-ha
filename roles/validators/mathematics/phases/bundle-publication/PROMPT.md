# Bundle Publication Phase Prompt

Assemble only validated findings from the coordinator ledger. Resolve anchors, confirmation paths, and artifact references. Preserve exact statuses and limitations. Reject scores, recommendations, unsupported severity, identity changes, and any Lean result that conflates proof acceptance with formalization fidelity.
